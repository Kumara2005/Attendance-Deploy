# Server Configuration
server.port=${PORT:8080}

# CORS Configuration (Frontend ports)
cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:3003,http://localhost:5173}

# Database Configuration
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:attendance_db}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:vvkumaran_2005}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:9K7mP3vX8zR2nQ5tL6wY4bN8jH1fD9gS3aU7cV2eM5pT8xW4qA6rE3nK9mL2oP7v}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Logging
logging.level.root=INFO
logging.level.com.attendance=DEBUG
